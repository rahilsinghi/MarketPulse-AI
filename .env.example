# Required - OpenAI API key for embeddings & GPT-4
OPENAI_API_KEY=sk-...

# Optional - Finnhub API key for live news feed
# Only needed if MARKETPULSE_MODE=live
FINNHUB_API_KEY=

# Optional - ingestion mode (default: dummy)
# Set to 'live' to use Finnhub feed (requires API key)
MARKETPULSE_MODE=dummy